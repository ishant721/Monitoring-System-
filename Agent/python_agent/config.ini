[Agent]
# The API Key must match the one in your Django settings.py.
# This is used for all communication AFTER the agent is paired.
api_key = your-secure-api-key-here

# The base URL of your Django server.
# The script will derive the WebSocket and API URLs from this.
base_url = http://127.0.0.1:8000